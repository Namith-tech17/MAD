export const gameSettings = {
  // 🔊 Sound effects (move, win, lose)
  sound: true,

  // 🎵 Background music (future use)
  music: false,

  // 🎯 Difficulty levels
  // Affects enemy speed in GameScreen
  difficulty: 'Medium', // 'Easy' | 'Medium' | 'Hard'

  // 🎮 Control type
  // Currently only Buttons implemented
  control: 'Buttons', // 'Buttons' | 'Joystick'

  // 🌙 UI theme (future use)
  darkMode: true,
};